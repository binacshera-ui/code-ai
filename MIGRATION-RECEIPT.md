# קבלת העברה — Code AI

עודכן: 30.08.2026

## ההחלטה

Code AI הוא מוצר עצמאי. הנתיב הקנוני החדש שלו הוא:

`/root/projects/code-ai`

הוא אינו חלק מריפו בינה כשרה ואינו נכנס ל־`bina-cshera-root`. הקשר שלו
למערכת הוא דרך הדומיין, פורט 4000 והחוזים שלו — לא דרך המיקום במונוריפו.

## מה כבר הושלם

- הקוד, קובצי הסביבה, סביבת Python, דפדפני Playwright וכל מצב `.code-ai`
  הועתקו ליעד החדש. העתקת המצב הראשונית כללה כ־58.3GB.
- הסודות נשמרו מקומית ב־`.env` בהרשאות `600`, אך חסומים מ־Git.
- נוספה שכבת נתיבים מפורשת ב־`.env.paths`: שורש האפליקציה והמצב נמצאים
  בריפו החדש; ה־workspace שאותו Code AI מנהל נשאר `/root/projects/bina-cshera`.
- בוצעו build ובדיקות runtime, queue, session transport ו־Personal Chrome.
- מועמד מבודד על פורט 4100 עלה, החזיר HTML ו־asset תקינים, שמר חוזה 404
  JSON ל־API לא מוכר ונסגר בצורה נקייה.
- Nginx לא דרש שינוי: הוא מפנה ל־`127.0.0.1:4000` ולא לנתיב קבצים.

## המעבר החי

`ops/cutover-from-monorepo-when-idle.mjs` מבצע את המעבר רק לאחר 45 שניות
רצופות ללא פריטי תור פעילים וללא תהליכי Codex, Claude או Gemini מתחת
לשירות. לפני העצירה הוא מרענן קוד, build ומצב; אחרי עצירה חיננית הוא
מסנכרן מצב בפעם האחרונה, מפעיל מן הנתיב החדש ומאמת PM2, דף ראשי, asset
ו־404 API. כישלון מחזיר אוטומטית לנתיב הישן.

מצב ההפעלה המדויק נשמר מקומית בקובץ:

`/root/projects/code-ai/.code-ai/ops/path-cutover.status.json`

## כלל אי־מחיקה

המקור `/root/projects/bina-cshera/web/code-ai` נשאר בשלמותו. הוא אינו נמחק
בשלב הזה. לאחר מעבר מוצלח, מצב הריצה הקנוני יהיה בריפו החדש; המקור הישן
יישאר עותק rollback עד החלטה מפורשת ונפרדת.

